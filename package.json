{
  "name": "gmocoin-api-wrapper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "npm run check:src && npm run check:test",
    "check:src": "tsc -p tsconfig.json --pretty false",
    "check:test": "tsc -p test/tsconfig.json --pretty false",
    "smoke": "node --experimental-strip-types --eval \"import('./src/index.ts')\"",
    "test": "npm run test:unit",
    "test:unit": "node --experimental-strip-types --test --test-isolation=none test/unit/client.test.ts test/unit/websocket.test.ts",
    "test:live:public": "node --experimental-strip-types --test --test-isolation=none test/integration/public.test.ts",
    "test:live:private:readonly": "node --experimental-strip-types --test --test-isolation=none test/integration/private-readonly.test.ts"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.9.0"
  }
}
